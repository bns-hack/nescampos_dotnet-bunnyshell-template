@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Mvc.Localization
@*@using PayWave.Areas.Identity.Data
@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <li>
        <a href="#">@Localizer["Cuenta"]</a>
        <ul>
            <li><a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@Localizer["Gestionar cuenta"]</a></li>
            <li>
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link2 btn btn-link loadingButton">@Localizer["Logout"]</button>
                </form>
            </li>
        </ul>
    </li>
    <li class="nav-item">

    </li>
    <li class="nav-item">

    </li>
}
else
{
    <li>
        <a asp-area="Identity" asp-page="/Account/Login">@Localizer["Login"]</a>
    </li>
}
*@